# 正式运动同步与手机历史技术基线

## 1. 文档状态

- 版本：0.2
- 日期：2026-08-14
- 对应任务：S9-006 至 S9-010、S10-001 至 S10-007
- 当前结论：正式运动同步和手机体验的自动测试、已配对 Simulator 端到端同步与基础人工界面验收完成，真机传输仍待执行

本文只落实算法无关的正式运动事实、手机长期历史和既定页面结构。本文不授权击球检测、杀球分类、杀球测速或个人校准有效性判断。

## 2. 任务边界

输入：

- Apple Watch 已有 `BadmintonWorkoutRecord` schema v1；
- 手表运动开始、暂停、继续、结束和异常检查点；
- 研发同步已经验证过的后台文件、回执、重试和幂等设计；
- 正式手机端页面结构和本地存储要求。

输出：

- 手表终态运动待同步队列；
- 单文件不可变传输快照；
- 手机端字节数、UUID、schema、终态和冲突校验；
- 手机完整导入后的回执；
- 手机本地运动历史；
- 首次使用、个人资料、首页、历史、单场报告、个人纪录空状态、校准状态、设置与隐私说明；
- 只在 Simulator 编译的显式模拟运动入站入口；
- 自动测试与 GitHub 持续验证。

不包含：

- 真实设备 WatchConnectivity 交付结论；
- HealthKit 真机成功结论；
- 击球记录、杀球数据或速度数据；
- 用 Simulator 数据形成用户个人纪录；
- 用户账号、云端或服务器。

## 3. 正式运动同步协议

当前正式基础运动只有一个版本化 `workout.json`，因此每场终态运动使用一个原子文件传输。只有 `completed` 或 `interrupted` 可以进入待同步队列，`starting`、`active` 和 `paused` 一律拒绝传输。

传输元数据：

- `message_type = workout_file`；
- `workout_id`；
- `schema_version`；
- `byte_count`。

当手机实时可达且原子运动文件不超过 48 KiB 时，手表还会发送一个
`workout_immediate` 消息，用于尽快刷新手机历史。该消息仍使用同一份
`workout.json`、同一组完整性校验和同一个回执，不形成第二套运动事实。
无论即时消息是否成功，后台 `transferFile` 仍会排队，因此手机离线时的
本地保存、以后重试和最终一致性不依赖实时连接。

回执：

- `message_type = workout_acknowledgement`；
- `workout_id`；
- `schema_version`。

未来加入击球明细时，必须升级为一个版本化原子运动包，或者在手机确认全部组成部分一致后整体导入。不得让统计与击球明细部分成功、部分缺失。

## 4. 手表状态与离线原则

同步状态与运动事实分开存储，避免为了同步机制升级而修改已保存的 schema v1 运动事实。

```text
pending_transfer
      ↓ 系统已接受后台文件
transferred
      ↓ 传输错误
pending_transfer

pending_transfer 或 transferred
      ↓ 手机完整导入并返回匹配回执
acknowledged
```

- 手机是否在线不影响开始或完成运动；
- 手机确认前手表保留本地运动；
- `transferred` 但未收到回执的运动允许再次排队；
- 已确认运动不再重复排队；
- 背景传输使用不可变快照，快照生命周期不依赖源文件；
- 平台报告传输错误时回到待重试状态。

## 5. 手机导入规则

手机收到系统临时文件后先复制到自己的暂存目录，再交给共享入站模块。

导入前必须验证：

- 实际字节数与元数据一致；
- schema 不高于当前支持版本；
- 文件内 UUID 与元数据一致；
- 文件内 schema 与元数据一致；
- 运动状态已经结束或中断；
- 运动记录自身校验通过。

相同 UUID 且完整运动事实相同视为幂等重复，并可再次返回回执。相同 UUID 但事实不同视为冲突，拒绝覆盖手机已有历史且不发送回执。

## 6. 手机长期存储与资料

手机使用每场运动一个目录的原子 JSON 本地存储。当前不建立数据库、账号或云端，因为第一版数据量和查询需求不要求引入新的依赖。若未来性能证据要求迁移，必须保留 UUID、schema、来源和历史结果稳定性。

正式个人资料独立使用 `BadmintonUserProfile` schema v1：

- 可选身高；
- 可选臂展；
- 可选、版本化含义的水平代码；
- 首次使用是否完成；
- 可选校准完成时间与版本。

必填/可选仍是待确认产品决定，所以当前存储允许资料留空，首次使用允许稍后填写。校准时间与版本必须同时存在，不允许只保存其中一个。

## 7. 手机页面

当前正式手机端已经包含：

- 首次使用说明、持拍手提示、权限和隐私说明；
- 首页最近运动、已保存运动数量、个人纪录空状态、校准状态；
- 历史运动列表；
- 单场报告的运动时间、健康指标和写入状态；
- 击球明细门禁空状态；
- 个人资料编辑；
- 个人动作校准说明与未开放状态；
- 设置和本地数据说明。

正式基础运动 schema 没有击球字段，因此总击球、杀球、速度和个人纪录使用“—/暂无”，不写入零值或模拟值。

## 8. Simulator 验收通道

正式 iPhone target 只在 Simulator 编译条件下显示“导入一场模拟运动”。该入口创建确定性的健康摘要并通过与正式同步相同的入站校验写入手机历史。

强制规则：

- 来源为 `simulator_synthetic`；
- 健康写入状态为 `simulated_not_saved`；
- 界面显示橙色模拟标识；
- 不生成击球、杀球或速度；
- 真机和发布构建不包含该入口。

## 9. 当前自动验证

- BadmintonCore 43 项测试通过；
- 正式传输覆盖终态筛选、重试、回执、重复、冲突、字节数、身份、schema 和不可变快照；
- 即时传输消息覆盖元数据与原子运动文件的无损往返，后台文件仍是可靠后备；
- 个人资料覆盖可选字段、原子往返和无效数据拒绝；
- 正式 iPhone 与 Watch Simulator SDK 构建通过，并校验 iPhone 产物实际内嵌正确配套的 Watch App；
- GitHub Actions 在推送和拉取请求上运行完整 `Scripts/verify.sh`。

2026-08-14 已在配对的 iPhone 17 与 Apple Watch Ultra 3（49 mm）
Simulator 上完成以下人工回归：

- 手机首次使用、模拟入站、首页、历史、单场报告、个人资料、校准门禁、设置和滚动；
- iPhone 深色模式与最大辅助字体首页；辅助字体下运动日期、来源、时长和能量改为纵向布局；
- 手表开始、基础指标页、杀球不可用页、暂停、继续、结束确认、摘要和完成；
- 两场此前等待发送的手表运动通过即时通道进入手机长期历史；
- 手机完整导入后返回回执，手表两条状态均由 `transferred` 变为 `acknowledged`；
- 重复后备传输不会生成重复历史；
- 模拟器运动始终标记为模拟，击球、杀球和速度始终显示不可用，没有伪造数据。

Xcode 27 beta 的配对 Simulator 在本次环境中没有单独完成后台
`transferFile` 的跨设备投递，因此该结果证明的是“已配对且实时可达”的
双端协议闭环，不替代手机离线、应用被终止和系统后台调度的真机验收。

## 10. 真机待验

1. 手表结束运动后后台文件是否进入系统队列；
2. 手机关闭、离线、重启后是否最终收到；
3. 成功、失败、重复和回执丢失时的真实系统回调；
4. 大量连续运动的传输调度和本地空间；
5. 真实 HealthKit UUID、心率、能量和运动写入后的手机展示；
6. 真机首次使用、资料编辑、历史和报告的人工交互，以及 Watch 动态字体和完整 VoiceOver/可访问性复验；
7. iPhone 与 Apple Watch 最终签名、Bundle Identifier 和最低系统版本。

这些项目未通过前，不得把本阶段描述为真机完成。
