# 羽毛球运动分析应用——开发代理总规范

> 本文件放置于项目根目录，作为所有编码代理、自动化开发工具和后续开发者进入项目时必须首先阅读的最高级开发执行规范之一。
>
> 本文件的职责不是穷尽所有产品细节，而是规定：项目是什么、当前第一版做什么、明确不做什么、文档如何解释、编码代理必须按照什么顺序工作、每个阶段的完成条件是什么，以及遇到不确定事项时如何处理。

---

## 1. 项目目标

本项目是一款以苹果手表为核心运动传感设备、以苹果手机为主要查看与管理终端的羽毛球个人表现分析应用。

第一版产品同时承担三类价值：

1. 基础羽毛球运动记录：记录运动时间、心率、活动消耗热量等基础运动健康信息，并写入苹果健康与健身记录体系。
2. 羽毛球击球记录：在用户正常打球过程中自动检测真实击球，并将击球区分为“杀球”和“普通击球”。
3. 杀球表现分析：对高可信度杀球估算羽毛球初始速度，记录单场杀球数据，并在手机端形成历史记录和个人纪录。

产品不是单纯的运动记录器，也不是单纯的杀球测速器。其核心定位是：

> 在尽量不打扰用户正常打球的前提下，通过苹果手表持续记录身体运动与持拍手运动，将基础运动数据、击球数量和杀球表现整合成一个长期可追踪的羽毛球个人表现系统。

---

## 2. 第一版核心产品原则

编码代理在任何实现决策中都必须遵守以下原则。

### 2.1 手表优先

运动必须能够只依靠苹果手表完整完成。

用户可以不携带苹果手机进入球场。手机不在附近时，以下能力仍必须可以正常运行：

- 开始羽毛球运动；
- 记录运动时间；
- 记录运动健康数据；
- 采集手腕运动传感器数据；
- 检测真实击球；
- 区分杀球与普通击球；
- 对满足条件的杀球估算速度；
- 提供杀球震动反馈；
- 结束并保存完整运动。

手机重新出现后再完成数据同步。

任何实现不得把手机实时连接设为运动功能的必要条件。

### 2.2 无干扰运动

正常使用过程中，不要求用户频繁操作手表。

用户开始运动后，击球检测、杀球识别和杀球测速必须自动进行。

第一版不要求用户手动记录比分、回合、球路或每次击球结果。

### 2.3 保守判断优先

杀球识别和杀球测速都必须优先控制误报。

产品原则：

> 宁可漏掉一部分模糊的杀球，也不要把普通击球错误识别为杀球并生成虚假的高速纪录。

如果一次击球可以高可信度判断为杀球，但测速可信度不足：

- 可以计入杀球数量；
- 不显示该次速度；
- 不纳入最快杀球；
- 不纳入平均杀球速度；
- 不纳入个人最快纪录。

### 2.4 不伪装为专业测速设备

杀球速度属于基于手腕运动、个人参数和模型推断得到的估算结果，不是雷达或高速摄影的直接测量结果。

界面可以显示到小数点后一位，例如：

`278.4 公里/小时`

但小数点后一位只是显示精度，不代表真实误差达到每小时 0.1 公里。

不得在文案、代码注释、测试说明或产品逻辑中把估算值描述为“精确实测值”。

### 2.5 产品范围严格受控

编码代理不得因为“顺手”“容易实现”或“看起来有用”而自行增加第一版之外的能力。

新增功能必须先进入产品决策流程，再进入开发任务。

---


### 2.6 数据优先，禁止算法占位

正式核心算法必须建立在真实苹果手表数据和对应实验结论上。

在内部数据采集测试版和真机数据验证通过前，编码代理不得：

- 使用临时阈值冒充正式杀球识别；
- 使用固定系数冒充正式杀球测速；
- 生成随机、预设或人为“看起来合理”的杀球速度；
- 把模型自己的预测结果写成永久真实标签；
- 用无真实测速依据的值填入真实速度字段。

项目允许先实现研发采集、保存、同步、查看和导出工具，再开发算法。


## 3. 当前已确定的第一版功能范围

### 3.1 必须实现

第一版必须包含：

- 苹果手表端羽毛球运动开始、暂停、继续、结束；
- 基础运动时间记录；
- 实时心率记录；
- 平均心率和最高心率统计；
- 活动消耗热量及必要的运动健康数据；
- 将有效运动写入苹果健康与健身记录体系；
- 持拍手运动传感器持续采集；
- 疑似挥拍检测；
- 真实击球确认；
- 击球总数统计；
- “杀球 / 普通击球”二分类；
- 杀球数量统计；
- 高可信度杀球速度估算；
- 杀球识别后的轻微震动反馈；
- 手表运动中查看当前杀球数据；
- 可选个人动作校准；
- 普通击球 10 次有效样本校准；
- 杀球 10 次有效样本校准；
- 无效校准样本自动要求补打；
- 手机与手表运动数据同步；
- 手机端本地保存运动历史；
- 手机端查看单场运动报告；
- 手机端查看每一次击球明细；
- 手机端查看历史运动；
- 手机端查看个人杀球纪录；
- 异常中断后的数据保护与恢复；
- 手机不在附近时的独立运动能力。

### 3.2 第一版明确不做

第一版禁止自行加入以下功能：

- 网球；
- 具体羽毛球球路细分；
- 高远球、吊球、平抽球、发球等单独分类；
- 比分记录；
- 自动回合识别；
- 比赛胜负分析；
- 疲劳分析；
- 自动训练建议；
- 技术动作纠正建议；
- 专门的杀球训练模式；
- 独立的普通运动模式与专项训练模式；
- 球拍资料管理；
- 球拍数据库；
- 不同球拍之间的数据比较；
- 云端账号；
- 自建服务器；
- 好友系统；
- 排行榜；
- 社交竞技；
- 摄像头测速；
- 摄像头动作分析；
- 麦克风作为第一版核心识别依赖；
- 自动重算并修改旧运动的历史结果；
- 用户逐条纠正历史击球分类；
- 自动持续重新训练完整个人模型；
- 面向用户展示“挥拍强度分数”；
- 面向用户展示“击球质量分数”。

如未来文档正式改变上述范围，以最新产品决策记录为准。

---


### 3.3 正式产品开发前的内部研发工具

内部数据采集测试版属于强制研发基础设施，不属于正式第一版用户功能。

至少需要支持：

- 单次普通击球、杀球和空挥采集；
- 普通击球批量标注采集；
- 杀球批量标注采集；
- 自由连续打球采集；
- 干扰动作采集；
- 原始三轴加速度和三轴角速度；
- 可获得的姿态与设备运动数据；
- 独立时间戳和实际采样间隔；
- 手机不在线时本地保存；
- 手机端原始曲线查看；
- 样本有效、无效、待检查标记；
- 外部真实测速值录入；
- 研发数据导出。

采集阶段由人提供动作标签，系统负责忠实记录。未验证模型不得覆盖人工标签。

内部研究入口不得进入正式发布版本。


## 4. 目标用户与使用条件

### 4.1 目标用户

第一版主要服务：

- 普通羽毛球爱好者；
- 稳定参加日常羽毛球活动的用户；
- 希望量化自身击球与杀球表现的进阶业余用户。

第一版不以职业选手、专业教练或科研机构作为主要目标对象。

### 4.2 手表佩戴要求

用户运动时必须将苹果手表佩戴在持拍手。

产品在开始运动及个人动作校准前，应明确提示：

> 请确认手表佩戴在持拍手，并保持表带稳固贴合。

第一版不要求用户额外填写“左手持拍”或“右手持拍”。

底层实现必须通过设备佩戴信息、坐标标准化和模型设计处理左右手差异，不应把该复杂度转嫁给普通用户。

### 4.3 最低设备范围

当前产品要求暂定：

> 最低支持苹果手表第 9 系列。

具体最低系统版本、设备传感器能力和兼容范围必须在技术验证阶段进一步确认，并记录到兼容性文档中。

编码代理不得未经验证自行扩大或缩小支持范围。

---

## 5. 用户基础资料

用户资料的第一版目标是“只收集对动作识别或测速有明确价值的信息”。

当前计划至少考虑：

- 身高；
- 臂展；
- 羽毛球水平；
- 是否完成个人动作校准；
- 最近一次个人动作校准时间；
- 当前个人校准版本。

其中哪些字段最终设为必填、哪些字段设为可选，应以正式产品需求文档为准。

不得自行增加以下资料要求：

- 球拍型号；
- 球拍重量；
- 平衡点；
- 线材；
- 磅数；
- 详细身体测量项目；
- 不影响当前第一版功能的个人资料。

---

## 6. 个人动作校准

### 6.1 产品定位

个人动作校准是可选增强功能，不是进入应用的强制门槛。

没有校准时，系统使用通用动作识别能力。

完成校准后，系统可以利用个人普通击球和杀球基线改善后续判断。

### 6.2 校准要求

校准必须是真实击球，不接受纯空挥作为正式校准样本。

流程暂定：

1. 用户进入个人动作校准；
2. 系统提示完成 10 次真实普通击球；
3. 只计算被确认有效的样本；
4. 无效样本不计数并提示补打；
5. 普通击球获得 10 个有效样本后进入下一阶段；
6. 用户完成 10 次真实杀球；
7. 无效样本同样补打；
8. 完成后生成或更新个人动作基线；
9. 校准结果本地保存；
10. 用户以后可以主动重新进行校准。

### 6.3 第一版个人化方式

第一版优先采用轻量个人基线和阈值修正，而不是为每个用户重新训练一个完整模型。

禁止第一版实现以下行为：

- 每次运动结束后自动重新训练完整模型；
- 直接使用模型自己的预测结果作为永久正确标签循环训练；
- 在没有可靠标签的情况下自动“自我学习”。

必须避免错误预测反复强化自身的闭环。

---

## 7. 运动中的手表体验

### 7.1 统一运动入口

第一版只有一个主要运动入口：

> 开始羽毛球运动

不要再拆分为“自由打球”“专项训练”“比赛”“杀球训练”等模式。

正常运动中同时完成基础记录、击球计数、杀球识别和测速。

### 7.2 手表第一页

第一页以基础运动信息为主。

当前核心信息：

- 运动时间；
- 当前心率；
- 活动消耗热量；
- 总击球数。

实际排版以正式界面与交互规范为准。

### 7.3 手表第二页

第二页以杀球信息为主。

当前核心信息：

- 本场杀球数；
- 最近一次成功测速的杀球速度；
- 本场最快杀球速度。

### 7.4 杀球即时反馈

当系统高可信度识别到一次杀球时：

- 产生一次轻微震动；
- 杀球数量增加；
- 如果测速成功，更新最近杀球速度；
- 如果测速失败，不制造速度值；
- 不弹出需要用户确认的界面；
- 不打断当前运动。

第一版不进行语音播报。

---

## 8. 手机端职责

手机主要负责：

- 用户资料；
- 个人动作校准管理；
- 手表数据接收与本地持久化；
- 单场运动报告；
- 历史运动列表；
- 每次击球明细；
- 个人杀球纪录；
- 权限与设置；
- 必要的数据导出或开发调试能力（仅研发模式）。

手机不应成为运动过程中实时推理的强制依赖。

---

## 9. 单场运动报告

手机端单场报告至少应包含：

- 运动时间；
- 总击球数；
- 杀球数；
- 最快杀球速度；
- 平均杀球速度；
- 平均心率；
- 最高心率；
- 活动消耗热量；
- 必要的健康记录摘要；
- 单次击球明细入口。

报告第一屏优先突出：

1. 运动时间；
2. 总击球数；
3. 杀球数；
4. 最快杀球速度；
5. 平均心率或活动消耗热量。

最终视觉层级以界面与交互规范为准。

---

## 10. 单次击球数据原则

每一次被确认的真实击球都应形成一条独立击球记录。

至少应能够表达：

- 唯一编号；
- 所属运动编号；
- 击球发生时间；
- 击球类型：普通击球或杀球；
- 识别可信度；
- 是否完成测速；
- 估算杀球速度；
- 测速可信度；
- 使用的算法版本；
- 必要的运动特征；
- 如存储策略允许，对应的短时原始传感器片段引用。

普通击球第一版用户侧只显示数量，不显示速度、挥拍强度或击球质量评分。

所有内部数值单位必须在数据定义文档中统一。

---

## 11. 杀球速度规则

### 11.1 前置条件

只有被高可信度判断为杀球的击球才进入杀球测速流程。

### 11.2 输出规则

测速模型输出至少包括：

- 估算速度；
- 测速可信度；
- 是否允许向用户展示。

如果可信度不足：

- 不显示虚构速度；
- 不使用固定默认值；
- 不以最近一次速度填充本次结果；
- 不纳入统计。

### 11.3 显示规则

成功测速后用户侧统一显示：

> 小数点后一位，单位为公里/小时。

例如：

`278.4 公里/小时`

内部存储精度不得被界面显示精度限制。

### 11.4 禁止的简化实现

未经研究与实验验证，禁止将以下方案作为正式杀球测速算法：

- 手腕最大加速度乘固定系数；
- 手腕最大角速度乘固定系数；
- 单一阈值映射速度；
- 直接积分原始加速度并把结果当成拍头或羽毛球速度；
- 为了演示效果生成随机或人为范围内的速度；
- 根据用户水平预设一个看起来合理的速度。

算法必须来自可解释的特征、实验数据和真实测速基准。

---

## 12. 击球识别处理链

第一版标准处理链应遵循以下逻辑：

1. 持续采集手腕运动数据；
2. 发现疑似挥拍事件；
3. 截取相应时间窗口；
4. 判断是否发生真实击球；
5. 排除跑动、捡球、甩手、空挥等非击球行为；
6. 对真实击球执行“杀球 / 普通击球”分类；
7. 高可信度杀球进入测速流程；
8. 形成击球记录；
9. 更新手表当前运动统计；
10. 分段持久化，避免应用异常导致整场数据丢失。

编码代理不得把“疑似挥拍”直接等价为“真实击球”。

---

## 13. 健康与运动数据

第一版基础运动体验应参考苹果自带羽毛球运动记录的核心能力。

至少考虑：

- 运动时长；
- 当前心率；
- 平均心率；
- 最高心率；
- 心率相关摘要；
- 活动消耗热量；
- 总消耗热量（如果系统能力和数据定义采用）；
- 其他对运动会话必要的数据。

有效运动结束后必须进入苹果健康与健身记录体系。

权限被拒绝时必须有明确降级逻辑，不得静默失败。

具体健康数据权限、读写范围和实现方式以健康接入规范为准。

---

## 14. 数据存储原则

第一版不建立自有云端账户和服务器。

### 14.1 手表端

手表负责：

- 当前运动实时状态；
- 运动过程中的必要缓存；
- 手机不可用时的完整本地保存；
- 待同步运动队列；
- 异常恢复所需检查点。

不得假设运动结束时手机一定在线。

### 14.2 手机端

手机作为长期本地历史数据主存储端。

至少长期保存：

- 用户资料；
- 个人动作校准数据；
- 运动记录；
- 击球记录；
- 杀球测速结果；
- 个人纪录；
- 相关算法版本。

原始传感器数据的长期保留范围必须以数据存储规范为准。

在该规范正式确定前，编码代理不得擅自永久保存整场高频原始传感器数据，也不得擅自全部丢弃具有后续算法研发价值的击球窗口数据。

### 14.3 历史数据稳定性

算法升级后，第一版默认不自动重算过去的运动记录。

过去已经向用户展示并保存的速度与纪录保持当时结果。

如果未来产品决定支持历史重算，必须另行形成产品决策。

---

## 15. 手机与手表同步原则

同步设计必须满足：

- 手机不在线时不影响运动；
- 每场运动具有全局唯一标识；
- 同一场运动重复同步不能产生重复记录；
- 同步失败可安全重试；
- 同步成功后双方状态明确；
- 不允许部分同步导致统计与击球明细不一致；
- 数据冲突必须有明确的主从策略；
- 当前第一版原则上以手表产生的运动事实数据为源，手机负责长期存储和展示。

具体通信接口、批次大小、重试策略和传输格式以两端通信规范为准。

---

## 16. 异常与恢复原则

必须重点覆盖：

- 手机不在附近；
- 手机连接中断；
- 手表应用进入非预期状态；
- 应用被系统终止；
- 运动会话中断；
- 健康数据权限不足；
- 传感器暂时不可用；
- 同步失败；
- 同一运动被重复提交；
- 校准中途退出；
- 校准样本无效；
- 杀球分类成功但测速失败；
- 运动数据部分损坏。

任何关键运动数据不得只存在于内存中直到运动结束才一次性保存。

应进行增量或检查点式持久化。

---

## 17. 文档体系与优先级

编码代理开始任何任务前，必须优先查看与任务相关的现有文档。

建议项目文档体系：

1. 产品总纲；
2. 产品需求文档；
3. 第一版功能范围；
4. 产品决策记录；
5. 功能需求清单；
6. 用户流程；
7. 手机端页面结构；
8. 手表端页面结构；
9. 界面与交互规范；
10. 数据定义；
11. 系统架构；
12. 传感器采集规范；
13. 击球识别算法规范；
14. 杀球测速算法规范；
15. 个人动作校准规范；
16. 数据存储规范；
17. 手机与手表通信规范；
18. 健康数据接入规范；
19. 测试与验收规范；
20. 开发任务规划；
21. 已知问题与技术债。

### 17.1 冲突处理原则

如果不同文档发生冲突，优先级原则为：

产品总纲
→ 最新产品决策记录
→ 产品需求文档
→ 第一版功能范围
→ 功能需求清单
→ 界面与交互规范
→ 数据定义
→ 算法规范
→ 系统架构与工程规范
→ 当前任务说明

如果仍无法判断，不允许编码代理自行做不可逆产品决策。

应把问题记录为“待产品确认”，并继续完成不依赖该决策的其他工作。

---

## 18. 编码代理的强制工作流程

任何编码任务都必须遵循以下顺序。

### 18.1 第一步：确认任务边界

开始编码前必须明确：

- 当前任务属于哪个开发阶段；
- 对应哪个功能编号；
- 输入是什么；
- 输出是什么；
- 验收条件是什么；
- 会影响哪些现有模块；
- 是否需要修改数据结构；
- 是否涉及手机与手表双方。

不得在范围不清楚时自行扩大任务。

### 18.2 第二步：阅读现有实现

必须先查看：

- 相关目录；
- 已存在的数据模型；
- 已存在的服务层；
- 当前测试；
- 与任务相关的手机或手表代码。

禁止因为没有立即看到实现，就直接新建一套重复能力。

### 18.3 第三步：制定最小修改方案

优先：

- 复用已有模块；
- 扩展现有数据结构；
- 保持职责清晰；
- 限制改动范围。

不得为了一个局部功能无理由重构整个项目。

### 18.4 第四步：实现

实现时必须遵守：

- 界面层不承载复杂业务逻辑；
- 运动采集、识别、测速、存储、同步相互解耦；
- 核心逻辑应可测试；
- 不把演示数据当正式数据；
- 不硬编码用于欺骗测试的输出；
- 不静默吞掉关键错误；
- 不在未说明情况下引入新的第三方依赖；
- 不未经批准改变已有公开数据结构。

### 18.5 第五步：测试

每个任务完成后至少执行：

- 相关单元测试；
- 相关集成测试；
- 构建检查；
- 对受影响路径的回归检查。

涉及手表传感器、运动会话、健康数据或设备通信的任务，模拟器通过不等于真实设备通过。

必须明确标记哪些项目仍需要真机验证。

### 18.6 第六步：更新文档

如果实现改变了：

- 数据结构；
- 模块接口；
- 用户流程；
- 算法版本；
- 已知限制；
- 测试标准；

则必须同步更新对应文档。

### 18.7 第七步：任务交付说明

编码代理完成任务后必须报告：

- 完成了什么；
- 修改了哪些文件；
- 新增或修改了哪些数据结构；
- 测试结果；
- 哪些内容已真机验证；
- 哪些仍未真机验证；
- 存在哪些已知问题；
- 是否产生技术债；
- 是否发现新的产品决策问题。

---

## 19. 编码代理禁止行为

以下行为默认禁止：

1. 擅自修改产品定位；
2. 擅自增加第一版未批准功能；
3. 擅自删除已确定功能；
4. 擅自改变“杀球 / 普通击球”产品分类；
5. 擅自增加更多击球类型；
6. 擅自添加比分、回合、疲劳、教练建议等功能；
7. 擅自让手机成为运动必须在线的依赖；
8. 擅自加入摄像头分析；
9. 擅自加入云端账户体系；
10. 擅自新增球拍管理；
11. 使用随机数或人为公式伪造杀球速度；
12. 将空挥直接计为真实击球；
13. 将疑似挥拍直接计为杀球；
14. 在低可信度情况下仍强制输出球速；
15. 为通过测试而硬编码固定结果；
16. 用临时模拟实现冒充完成正式功能；
17. 在未记录原因的情况下修改阈值或算法参数；
18. 未经批准自动重算用户历史运动；
19. 未经批准改变本地数据含义或单位；
20. 未经确认引入大型第三方依赖；
21. 在没有测试的情况下宣称算法准确；
22. 用同一用户的数据同时训练和测试，然后把结果描述为跨用户能力；
23. 把开发实验结果直接当成正式产品承诺；
24. 在无法确定需求时默默选择一个产品方案并永久写入代码。

---

## 20. 推荐模块边界

最终系统架构以正式系统架构文档为准，但编码代理应优先保持以下职责分离：

### 手表侧

- 运动会话管理；
- 健康数据采集；
- 运动传感器采集；
- 候选挥拍检测；
- 击球确认；
- 动作分类；
- 杀球测速推理；
- 当前运动统计；
- 震动反馈；
- 本地运动缓存；
- 待同步数据管理。

### 手机侧

- 用户资料；
- 校准管理；
- 长期本地存储；
- 同步接收；
- 运动报告；
- 击球明细；
- 历史记录；
- 个人纪录；
- 权限与设置。

### 共享层

- 数据对象定义；
- 单位与格式；
- 运动编号与击球编号；
- 同步数据结构；
- 算法版本标识；
- 共享业务规则。

不得把所有能力集中到单个超大文件或单个界面控制器中。

---

## 21. 开发阶段顺序

除非产品负责人明确调整，编码代理必须按以下顺序推进。

### 阶段 0：项目与文档基线

目标：建立受文档约束的工程基础。

完成：

- 手机和手表基础工程；
- 共享模块；
- 文档目录；
- 数据定义初稿；
- 系统架构初稿；
- 基础测试环境。

完成条件：

- 手机和手表最小应用可构建和运行；
- 文档入口清楚；
- 不实现伪算法。

### 阶段 1：内部数据采集测试版基础

目标：先建立能够长期产生研发数据的内部工具。

完成：

- 研发版手表采集入口；
- 单次击球采集；
- 普通击球批量采集；
- 杀球批量采集；
- 自由连续采集；
- 空挥与干扰动作采集；
- 测试者和采集元数据；
- 手表本地研发数据缓存；
- 手机端研发数据接收；
- 手机端研发数据列表。

完成条件：

- 真实手表可以创建和保存研发采集；
- 手机不在附近时不会因为断连而丢失该次采集；
- 不依赖任何杀球识别或测速算法。

### 阶段 2：高速传感器采集与真机验证

目标：确认苹果手表真实数据质量。

完成：

- 三轴加速度；
- 三轴角速度；
- 可获得的姿态和设备运动信息；
- 独立高精度时间戳；
- 实际采样间隔统计；
- 原始缓冲和落盘；
- 丢样和异常间隔统计；
- 疑似传感器饱和检查；
- 原始曲线查看；
- 研发数据导出。

真机至少采集：

- 普通击球；
- 杀球；
- 空挥；
- 跑动或其他干扰动作；
- 一段自由连续打球。

完成条件：

- 形成书面数据质量结论；
- 明确实际采样稳定性；
- 明确是否存在明显饱和或丢样；
- 数据足以支持下一阶段击球研究。

该阶段未通过，不得进入正式击球识别和测速。

### 阶段 3：第一批自有数据积累与实验闭环

目标：让产品负责人可以在日常打球时持续积累真实数据。

完成：

- 有效、无效、待检查标记；
- 备注；
- 测试者资料；
- 批量导出；
- 多次采集管理；
- 外部真实速度字段预留；
- 数据版本标识。

完成条件：

- 已形成第一批普通击球、杀球、空挥和自由打球数据；
- 数据可以被电脑端分析脚本直接使用；
- 原始数据、人工标签和元数据能够可靠对应。

### 阶段 4：正式产品基础运动记录

目标：建立可靠的羽毛球基础运动记录器。

完成：

- 正式手表运动入口；
- 开始；
- 暂停；
- 继续；
- 结束；
- 运动时间；
- 心率；
- 热量；
- 健康权限；
- 健康与健身体系写入；
- 异常结束保护。

完成条件：

- 真机完成完整基础羽毛球运动；
- 手机不在线时也能完成；
- 正确写入健康记录。

### 阶段 5：真实击球检测

目标：可靠回答“是否发生真实击球”。

处理链：

运动数据
→ 疑似挥拍
→ 候选时间窗口
→ 真实击球确认
→ 非击球过滤。

重点测试：

- 普通真实击球；
- 杀球；
- 空挥；
- 捡球；
- 甩手；
- 跑动；
- 日常手臂动作。

完成条件：

- 达到测试规范中的真实击球检出要求；
- 非击球误报达到可接受水平；
- 可以稳定生成正式击球记录。

### 阶段 6：杀球与普通击球识别

目标：只完成二分类。

完成：

- 通用动作模型；
- 输入标准化；
- 杀球可信度；
- 保守输出阈值；
- 普通击球归类；
- 模型版本记录。

完成条件：

- 必须包含按测试者分开的验证；
- 分别报告杀球误判和漏检；
- 不得只报告总体准确率。

### 阶段 7：个人动作校准

目标：在通用模型之上加入轻量个人化。

完成：

- 十次普通击球；
- 十次杀球；
- 无效样本补打；
- 个人动作基线；
- 重新校准；
- 通用模型与个人基线融合。

完成条件：

- 未校准用户仍可使用；
- 校准后不得明显退化；
- 校准数据可追踪版本；
- 不使用模型自己的预测作为永久正确标签循环训练。

### 阶段 8：真实测速数据采集与杀球测速研究

目标：建立有真实基准的杀球速度估算。

研发前提：

- 已稳定确认杀球；
- 已有手表原始数据；
- 已有真实测速设备或可靠参考方法；
- 能可靠把真实速度与手表样本配对。

先验证：

> 同一个人的不同强度杀球是否能够稳定映射到真实速度。

再研究：

> 不同用户在个人动作校准后是否能够使用统一或共享模型。

候选输入：

- 击球前完整手腕时间序列；
- 最终加速阶段；
- 角速度与加速度特征；
- 击球冲击特征；
- 身高；
- 臂展；
- 个人动作基线。

禁止：

- 单一峰值乘固定系数；
- 随机速度；
- 预设速度；
- 用估算值伪装真实测速；
- 直接积分未经验证的原始加速度并当作羽毛球速度。

完成条件：

- 与真实测速基准有明确误差评估；
- 有速度可信度机制；
- 异常值不会形成个人纪录；
- 达到测试与验收标准后才允许进入正式用户界面。

### 阶段 9：手机与手表完整同步

目标：将独立手表运动可靠转化为手机历史记录。

完成：

- 运动同步；
- 击球明细同步；
- 校准状态同步；
- 重试；
- 去重；
- 部分失败处理；
- 同步状态；
- 手机长期保存。

### 阶段 10：手机报告与历史体验

完成：

- 首页；
- 最近运动；
- 历史列表；
- 单场报告；
- 击球明细；
- 个人最快杀球；
- 用户资料；
- 校准入口；
- 权限与设置。

正式环境不得使用模拟数字冒充真实数据。

### 阶段 11：发布前质量收敛

重点：

- 长时间运动；
- 电量；
- 内存；
- 发热；
- 中断恢复；
- 传感器异常；
- 健康权限；
- 手机断联；
- 同步失败；
- 多场连续运动；
- 校准前后；
- 不同水平用户；
- 不同身体尺寸；
- 左右持拍手；
- 目标手表设备验证。

---

## 22. 阶段门禁原则

编码代理不得跳过前置研究门禁。

强制规定：

- 未完成内部数据采集测试版，不进入正式核心算法开发；
- 未完成真机高速数据质量验证，不进入正式击球模型训练；
- 未确认真实击球，不得把疑似挥拍直接算作正式击球；
- 未完成杀球识别验证，不进入正式杀球测速；
- 没有真实测速基准，不得向正式产品输出公里每小时杀球速度；
- 没有真机验证，不宣称运动、传感器或设备端推理完成；
- 同步没有通过断线和去重测试，不视为完成；
- 研发版人工标签不得被未验证模型自动覆盖；
- 正式发布版不得包含内部研究入口。

## 23. 测试基本原则

### 23.1 普通功能

每个功能必须具备可重复验收条件。

### 23.2 算法测试

严禁只报告总体准确率。

至少分别关注：

- 真实击球检出能力；
- 非击球误报；
- 杀球被正确识别的比例；
- 普通击球被错误识别为杀球的比例；
- 完全未参与训练的新用户表现；
- 个人校准前后变化；
- 杀球速度对真实测速值的误差。

### 23.3 数据划分

动作识别与测速实验必须避免同一个人的大量相似样本同时出现在训练和测试中，从而产生虚高结果。

最终产品能力必须包含“模型未见过的新用户”测试。

### 23.4 真机优先事项

以下内容必须真机验证：

- 手表运动会话；
- 心率与热量；
- 健康记录写入；
- 运动传感器采样；
- 高频运动数据；
- 震动；
- 手机离线情况下运动；
- 手机与手表同步；
- 异常中断恢复；
- 长时间运动性能。

---

## 24. 算法版本与可追溯性

每条涉及模型输出的运动和击球数据都应能够追溯当时使用的算法版本。

至少需要能够区分：

- 击球检测版本；
- 杀球分类版本；
- 杀球测速版本；
- 个人校准版本。

这样即使未来升级算法，历史记录仍可解释为何得到当时的结果。

第一版默认不自动重新计算历史数据。

---

## 25. 产品决策与技术决策边界

编码代理可以自行决定的内容：

- 在不改变功能行为的前提下优化内部代码结构；
- 补充测试；
- 修复明确缺陷；
- 提升性能；
- 改善错误处理；
- 选择等价且符合架构规范的内部实现方式。

编码代理不得自行决定：

- 新增用户功能；
- 删除用户功能；
- 改变第一版范围；
- 改变界面信息层级；
- 改变核心产品指标；
- 增加新的用户必填资料；
- 改变杀球识别产品定义；
- 改变测速是否展示的产品原则；
- 引入云端；
- 改变隐私策略；
- 改变历史数据处理策略；
- 改变最低支持设备。

这些必须进入产品决策记录。

---

## 26. 待后续正式文档确认的事项

以下事项目前不应由编码代理自行拍板：

- 身高、臂展、羽毛球水平中哪些为必填、哪些可选；
- 羽毛球水平的具体分级方式；
- 原始传感器数据在手机端保存多久；
- 是否保存整场原始数据，还是只保存击球窗口；
- 每次击球原始窗口的最终长度；
- 击球检测、杀球识别和测速的具体可信度阈值；
- 杀球测速达到什么误差水平才允许正式展示；
- 最低操作系统版本；
- 各型号手表的真实采样能力；
- 首页和报告的最终视觉细节；
- “无成功测速杀球”时第二页的最终占位文案；
- 平均杀球速度在样本过少时是否展示；
- 原始数据研发导出功能是否存在于正式版本。

在这些事项确认前，应通过可配置参数、清晰接口或隔离模块保持后续可调整性，而不是把某个未经确认的选择写死到多个模块中。

---

## 27. “完成”的定义

任何任务只有同时满足以下条件才可标记为完成：

1. 功能行为符合当前文档；
2. 没有引入未经批准的新产品行为；
3. 构建通过；
4. 自动测试通过；
5. 相关回归测试通过；
6. 必要的真机验证已完成，或明确标记未完成；
7. 错误路径得到处理；
8. 数据不会因为常见异常而无声丢失；
9. 文档同步更新；
10. 已知问题被明确记录；
11. 没有用模拟实现冒充正式实现；
12. 没有通过硬编码掩盖未完成算法。

---

## 28. 每次编码代理开始工作时的检查清单

开始前：

- [ ] 已阅读本文件；
- [ ] 已阅读当前功能对应的产品需求；
- [ ] 已阅读最新产品决策记录；
- [ ] 已确认当前开发阶段；
- [ ] 已确认功能编号和验收条件；
- [ ] 已检查现有实现；
- [ ] 已确认是否涉及数据结构变化；
- [ ] 已确认是否涉及手机与手表双方；
- [ ] 已识别需要真机验证的内容。

完成后：

- [ ] 构建通过；
- [ ] 相关测试通过；
- [ ] 没有加入范围外功能；
- [ ] 文档已更新；
- [ ] 已记录真机验证状态；
- [ ] 已记录已知问题；
- [ ] 已说明修改文件；
- [ ] 已说明是否影响数据结构；
- [ ] 已说明下一阶段依赖。

---

## 29. 项目长期原则

本项目后续可以逐步增加更丰富的羽毛球分析能力，但第一阶段必须优先建立可信的基础数据链：

> 内部真实数据采集 → 稳定传感器 → 真实击球 → 杀球识别 → 有基准的杀球测速 → 正式运动集成 → 稳定保存与同步 → 清晰报告。

任何看起来“更智能”的功能都不能建立在不可靠的数据之上。

对本项目而言，算法可信度、无干扰体验和数据完整性优先于功能数量。

当无法同时满足“功能更多”和“结果可信”时，默认选择后者。

---

## 30. 当前第一版的一句话定义

> 用户把苹果手表稳固佩戴在持拍手，只需开始一次羽毛球运动，系统即可在不依赖手机实时参与的情况下记录基础运动健康数据、自动统计真实击球、区分杀球与普通击球，并对可信杀球估算速度；运动结束后，完整数据同步到手机，形成单场报告、击球明细、历史记录和个人杀球纪录。

